Flask>=3.1,<4
# PostgreSQL driver, only used when DATABASE_URL is set (deployment)
psycopg[binary]>=3.2,<4
firebase-admin>=6.5.0
python-dotenv>=1.0.0
# Production web server used on Render
gunicorn>=23.0
